# Mistral API key — used by the worker proxy only, never exposed to the browser
MISTRAL_API_KEY=your_mistral_api_key_here

# Google Places API proxy URL — used by the React app to fetch ratings and dishes
REACT_APP_PLACES_API_URL=http://localhost:8787/places
